# Codex Cloud Canary

This is a harmless connectivity test for the configured Codex Cloud environment.

When invoked on the pull request for branch `ops/codex-cloud-canary-20260902`:

1. Create a new file at repository root named `CODEX_CLOUD_CANARY_RESULT.txt`.
2. Its entire contents must be exactly:

`CODEX_CLOUD_CANARY_OK`

followed by one newline.
3. Commit the change to this pull-request branch.
4. Make no other repository changes.
