# RC0 Run 1 Reconciliation

## Preserved execution

- workflow run: `35049854474`
- job: `104647642651`
- executed harness head: `cd5c83b74c5b1ddd80d67db4d2759d02639ef707`
- artifact: `10428637061`
- artifact digest: `sha256:ef6f1dc8f1ce1f529146adc91707babc1d445a3ebf784a2e413961f111b251e6`
- workflow conclusion: `failure`

Do not rewrite or relabel the RC0 artifact. The successor below is a new apparatus subject.

## What was observed

The exact repository checkouts and environment installation succeeded.

The following pressure steps succeeded:

- Evidence Bundler exact integration-boundary suite;
- current CAL focused integration/semantic suite;
- Contract C2 validator suite;
- Contract E RC3 successor 65-control suite.

The RC0 cross-repository canary stopped at ClaimGate. The arbitrary strict-comparison sentence

`Women had a higher rate than Men.`

produced the explicit fail-closed result:

`ABSTAINED / NO_UNIQUE_WARRANTED_DECLARATION:unsupported:NO_BOUNDED_FRAME_PARSE`

No Contract A object was emitted. This is consistent with ClaimGate's bounded authoring scope and therefore does not demonstrate an unsafe ClaimGate failure. RC0 made an invalid positive-canary assumption by requiring that out-of-profile sentence to become authoritative Contract A.

## Apparatus defects / overbroad wrappers

Two additional RC0 wrapper choices are not accepted as product failures:

1. **ClaimGate suite selection.** RC0 ran the whole repository pytest surface rather than the exact qualification-runbook subset prescribed by the frozen integration package. The successor returns to the prescribed maintained/qualification tests.
2. **Decision release-authority checkout.** RC0 checked out exact Contract C1 and Contract D release commits with shallow/tagless checkouts, while Decision's ingress/output authority verification explicitly requires the annotated release tags as part of authority. The successor fetches full tag authority for those two released checkouts.

The RC0 Decision nonpass is therefore not assigned a scientific disposition from this run. It will be discriminated by the successor with the required tags present.

## Successor design correction

Do not broaden ClaimGate to make the original canary pass.

RC1 separates two legitimate questions:

1. **True raw-claim fail-closed pipeline lane:** use a ClaimGate-supported single proposition, carry it through EB and into CAL as an unsupported semantic family, and require safe `NOT_CHECKABLE -> Decision HOLD` behavior with no operational authorization step.
2. **Downstream positive-control lane:** create a valid Contract A `not_decomposed` strict-comparison object using the released/qualified Contract A emitter as an explicit test fixture, then exercise EB -> CAL support -> C2 -> Decision CLEAR -> Contract D -> Contract E dry authorization pressure. This fixture is not claimed to be a ClaimGate semantic decision.

The current CAL -> Contract C2 producer-authority discriminator remains unchanged: current semantic implementation `847cc970...` must not inherit the frozen `a902621...` producer/policy resolver authority by assumption.

## RC0 disposition

`APPARATUS_INVALID_FOR_FULL_INTEROPERABILITY_CONCLUSION`

RC0 preserves useful negative evidence and successfully exercised several component pressure surfaces, but its canary and release-tag apparatus prevent a whole-pipeline scientific conclusion.
