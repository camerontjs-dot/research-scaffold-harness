# CONTEXT-FREE REQUIRED

# Contract E RC3D — Fresh Cross-Model Independent Reproduction

Use this packet as the complete task authority for the next independent reproduction.

This is a **fresh different-model-family** run. The intended local route is Antigravity / Gemini.

Do not import prior Contract E conversations, Grok reproductions, diagnostic reasoning, hidden evaluator apparatus, expected outcomes, or surrounding CAL Pipeline state.

---

## 1. Exact objective

Independently implement and test the frozen Contract E RC3D research authority/warrant consumer using only the six authorized public specification blobs below.

Determine whether a competent fresh implementation can independently recover:

- the common authority-domain behavior;
- authority-basis binding;
- RC3C currentness and canonical wire semantics;
- the RC3D public evaluation/request interface;
- native RegistryDocument consumption;
- propagation, delegation, and historical request behavior;
- result-payload opacity;

without seeing hidden vectors, fixture materialization rules, reference validators, prior implementations, expected outcomes, or the reasons RC3D exists.

Do not try to make the implementation agree with an imagined reference. Preserve ambiguity and disagreement.

This is research only. It is not authorization to define Contract E 1.0.0 or modify production behavior.

---

## 2. Independent implementation repository

Target repository:

`camerontjs-dot/research-scaffold-harness`

Authorized clean base:

`548bfa81f65290eda15af658f647497679b840ef`

Use a new isolated workspace and branch. Suggested branch:

`research/contract-e-rc3d-fresh-reproduction-gemini-<run-id>`

Existing Contract D or Contract E reproduction branches/PRs are not authorized conceptual inputs.

---

## 3. Complete public pre-freeze information aperture

Before implementation freeze, the independent implementer may read exactly these six immutable normative blobs plus this task packet.

### A. Authority / warrant candidate

`docs/research/contract-e/rc3a-authority-warrant-spec/SPEC-CANDIDATE.json`

Blob:

`9c1090335d87eb5e4885a755542923b453c45317`

### B. Structural shapes

`docs/research/contract-e/rc3a-authority-warrant-spec/SPEC-SHAPES.json`

Blob:

`c3f293430ae6ddb87523d83ea6e5380b8b832136`

### C. Participant boundary

`docs/research/contract-e/rc3a-authority-warrant-spec/SPEC-PARTICIPANT-BOUNDARY.json`

Blob:

`8b1d292a240300388949d502e7b656e7a23a0b8e`

### D. Authority-basis binding

`docs/research/contract-e/rc3b-authority-basis-binding/BASIS-BINDING-SPEC.json`

Blob:

`63c952c9c28f1be2173e69c79976c7dfe5880c10`

### E. RC3C currentness / canonical-wire amendment

`docs/research/contract-e/rc3c-native-wire-currentness/RC3C-SPEC.json`

Blob:

`f05feac88128fd693cca2fb25a0b2951654377eb`

### F. RC3D public consumer request interface

`docs/research/contract-e/rc3d-consumer-request-surface/RC3D-INTERFACE-SPEC.json`

Blob:

`61f46b09d391e7da4aed2491e428ec2ed226fe93`

The union of these six blobs is the complete pre-freeze semantic and public-wire authority.

When a later amendment explicitly supersedes an inherited point, the later amendment governs only that point.

---

## 4. Pre-freeze denylist

Before the fresh implementation freeze, do not access, search, inspect, summarize, or infer from any of the following.

### Prior reproductions and diagnostics

- `camerontjs-dot/research-scaffold-harness` PR #2;
- PR #3;
- PR #4;
- any Contract E Grok reproduction branch, commit, implementation, preregistration, test corpus, notes, comparison harness, or result;
- the post-falsification evaluator-dialect diagnostic or its results;
- another model's reproduction output.

### Hidden Contract E evaluator material

- RC3A `FROZEN-CASES.json`;
- RC3A validators/results;
- RC3B `AUTHORITY-BASIS-REGISTRY.json`;
- RC3B `FROZEN-BASIS-ATTACKS.json`;
- RC3B `HARDENING-PREREGISTRATION.md`;
- RC3B validators/results/workflow logs/artifacts;
- RC3C `PREREGISTRATION.md`;
- RC3C `FROZEN-CASES.json`;
- RC3C validator/results/workflow logs/artifacts;
- RC3D `PREREGISTRATION.md`;
- RC3D `R1-PREREGISTRATION.md`;
- RC3D `VECTOR-MATERIALIZATION-SPEC.json`;
- RC3D `FROZEN-CASES.json`;
- RC3D validator/results/freezing receipts/workflow logs/artifacts;
- expected answer sets or reason tables derived from those files.

### Project reasoning

- Apparatus Contracts PR #27, #42, #44, or #45 narratives/comments;
- the first failed RC3D hosted run;
- RC3C/RC3D design reasoning;
- CAL, Evidence Bundler, or Decision Engine authority implementations/results;
- cross-disciplinary authority research summaries;
- ChatGPT/CAL Pipeline conversation history or memory.

### Retrieval paths

- GitHub search or repository discovery;
- web search/browser tools;
- GitHub MCP/connectors;
- other local CAL Pipeline repositories;
- unrestricted filesystem paths outside the isolated workspace;
- package or documentation retrieval intended to discover project-specific behavior.

If any denied project-specific material is observed before freeze, stop and mark the run contaminated.

Provider transport required to use Gemini is not itself contamination. Information retrieval through that connection is forbidden.

---

## 5. Fresh implementer task

Using only the six public specification blobs:

1. Read every blob completely.
2. Before writing implementation code, create a preregistration recording your independent interpretation.
3. Identify ambiguities, unknowns, competing interpretations, load-bearing assumptions, and explicit falsifiers.
4. Implement a research-only native Contract E consumer from scratch.
5. Design and run your own adversarial and metamorphic tests.
6. Do not infer behavior from likely hidden tests or a presumed reference implementation.
7. Do not invent missing semantics and then describe them as contractual.
8. Where a local implementation choice is required under genuine ambiguity, record the choice and a discriminating post-reveal prediction before implementing it.

### Required stable evaluation surface

Expose a stable API or CLI that consumes the RC3D `EvaluationRequest` surface natively.

It must support at least:

- `kind=envelope` with full `RegistryDocument` wrapper;
- `kind=propagation` with canonical request object;
- `kind=delegation` with `ParentAuthorityRecord` + `DelegationChild`;
- `kind=historical` with the canonical historical modes.

The implementation must not require hidden fixture-construction vocabulary.

### Native-consumption requirement

Consume the public wire exactly enough that a caller can provide the canonical objects directly.

Do not depend on:

- hidden aliases;
- singular/plural coercion forbidden by RC3C;
- extraction of `RegistryDocument.records` by the caller;
- producer-specific translation;
- post-reveal field rewriting.

### Opaque semantic payload

Domain-local `result` content is opaque to the common authority evaluator. Positive, negative, indeterminate, success-like, or high-confidence result content must not manufacture common authority.

---

## 6. Required preregistration

Before implementation, create:

`docs/research/contract-e/rc3d-fresh-reproduction/PREREGISTRATION.md`

At minimum record:

1. purpose and bounded claim;
2. your model of authority, competence, warrant, jurisdiction, participant responsibility, propagation, delegation, currentness, and historical validity;
3. how RC3C and RC3D supersede inherited rules;
4. exact interpretation of `EvaluationRequest` and each `kind`;
5. exact interpretation of `RegistryDocument` and native wrapper consumption;
6. propagation field/mode behavior;
7. `ParentAuthorityRecord` versus `DelegationChild` semantics;
8. historical mode behavior;
9. reason taxonomy/precedence you treat as normative;
10. malformed/unknown behavior;
11. at least 20 adversarial or metamorphic tests you will implement;
12. explicit falsifiers;
13. fields that must not affect common authority;
14. unresolved ambiguities and predicted post-reveal discriminators.

Commit the preregistration before implementation.

Do not rewrite it after implementation. Append deviations separately if needed.

---

## 7. Minimum self-designed tests

The independent test corpus must include at least:

### Authority/basis

- subject substitution;
- domain substitution;
- operation substitution;
- scope substitution;
- target-class substitution;
- exact-target substitution;
- unresolved basis;
- authority-reference type mismatch;
- reference current false;
- resolved record current false;
- revocation boundary;
- validity interval boundaries.

### Competence / warrant

- competence present but authority absent;
- authority present but required competence absent;
- qualification type/current/subject/scope mismatch;
- warrant absent where required;
- warrant cross-domain;
- warrant wrong operation;
- warrant stale/inapplicable;
- warrant target/hash mismatch.

### RC3D public interface

- unknown `kind` rejects;
- unknown evaluation mode rejects;
- full RegistryDocument wrapper positive;
- malformed registry wrapper negative;
- registry key/record-id mismatch;
- propagation canonical `fields` positive;
- native `requested_fields` alias rejection;
- explicit propagation without `fields`;
- unknown propagation mode;
- forbidden semantic/authority propagation;
- required reestablishment behavior;
- positive parent-authority/child-delegation subset;
- parent-link mismatch;
- operation amplification;
- scope amplification;
- expiry amplification;
- non-current parent/child;
- historical inspection of prior valid authority;
- new exercise after revocation;
- unknown historical-mode token.

### Metamorphic

For multiple positive and negative envelopes, replace only the opaque `result` payload with materially different positive, negative, indeterminate, success/confidence-like, and nested arbitrary payloads. Common authority signature must not change.

Add further tests where your preregistration identifies ambiguity or risk.

---

## 8. Pre-reveal freeze

Before any hidden/reference vector is revealed:

- commit the input aperture;
- commit preregistration separately before implementation;
- commit complete implementation;
- commit all self-designed pre-reveal tests/fixtures;
- run the entire self-designed suite;
- preserve failures;
- record exact commits/trees/hashes and contamination status;
- make the implementation/test corpus immutable for comparison.

Create:

`docs/research/contract-e/rc3d-fresh-reproduction/FREEZE-RECEIPT.md`

The receipt must include:

- provider/model identity and Antigravity identity/version where available;
- clean base SHA/tree;
- input-aperture commit/tree;
- hashes of all six public blobs;
- preregistration commit/blob;
- implementation commit/tree and source hashes;
- tests commit/tree and test corpus hashes;
- exact test commands/counts/results;
- recorded ambiguities/unknowns;
- deviations;
- contamination status;
- exact terminal marker below.

Exact marker:

`FRESH_RC3D_IMPLEMENTATION_FROZEN_BEFORE_REFERENCE_VECTOR_REVEAL`

Once that marker/freeze is committed, STOP.

Do not access reference vectors yourself. Do not repair or amend the frozen implementation after reveal and count that as independent agreement.

---

## 9. Operator-side publication after freeze

After the child stops at a valid freeze, the operator/orchestrator may:

- independently verify the freeze SHA/tree and six input hashes;
- restore a GitHub remote;
- push the exact frozen branch;
- open a Draft Research PR against `main`;
- record the pre-reveal receipt.

Do not reveal hidden comparison material until the remote freeze is independently verified.

---

## 10. Authorized post-freeze reveal

Only after a valid pre-reveal freeze may the operator reveal exactly these evaluator artifacts.

### Inherited vectors / registry

1. RC3A frozen vectors
   - `FROZEN-CASES.json`
   - blob `85bc7805d02a04c5a10b48b43a5f5a89f4e2f32a`

2. RC3B authority-basis registry
   - `AUTHORITY-BASIS-REGISTRY.json`
   - blob `76ea333ee0460d9614e9899edb69e6865e48eccb`

3. RC3B direct attacks
   - `FROZEN-BASIS-ATTACKS.json`
   - blob `c726fb0ef914a850620e545131a70d427f4027bd`

4. RC3B compatibility-matrix preregistration
   - `HARDENING-PREREGISTRATION.md`
   - blob `1d85e2036d410b3af08d4b2b8926586da8fe6088`

5. RC3C successor vectors
   - `FROZEN-CASES.json`
   - blob `17d45524125814478b987bb8e91d23f545fb514e`

### RC3D evaluator authority

6. RC3D vector materialization specification
   - `VECTOR-MATERIALIZATION-SPEC.json`
   - blob `5c75e46a8eb4d7346128d84e21c25bdcea454ec4`

7. RC3D R1 hidden interface cases
   - `FROZEN-CASES.json`
   - blob `728b308d6eca0ebdf384e7de312c8a62b2f25577`

Reference validator implementations, generated reference RESULTS, RC3D preregistrations/design reasoning, workflow logs/artifacts, prior Grok reproductions, and the post-falsification diagnostic remain forbidden unless a later diagnostic phase is separately authorized.

---

## 11. Post-freeze comparison discipline

The comparison harness may be created after reveal, but it must call the frozen independent consumer natively.

The frozen RC3D materialization specification is the evaluator authority for translating fixture-construction DSL into canonical consumer requests.

That mapping must be applied independently of consumer output and must be auditable.

Do not:

- repair consumer semantics;
- add hidden aliases to the consumer;
- coerce malformed canonical wire;
- extract RegistryDocument records before calling the consumer if the public interface requires the wrapper;
- choose transformations based on observed output;
- change expected outcomes/reasons;
- count a repaired post-reveal implementation as independent agreement.

Compare at minimum:

- all inherited RC3A envelope/propagation/delegation/historical cases after frozen materialization;
- RC3B direct basis attacks;
- full 9 x 15 compatibility matrix;
- RC3B reference-type mutations;
- RC3C currentness/wire/delegation/relisted-reason cases;
- all RC3D R1 interface cases;
- materializer audit assertions;
- semantic result metamorphism.

Preserve every disagreement.

One authority-relevant false permit is a material failure.

---

## 12. Success conditions

Support promotion only if all are true:

1. no pre-freeze contamination;
2. the exact freeze marker precedes reveal;
3. the six public blobs are consumed without hidden design guidance;
4. public EvaluationRequest surfaces are implemented natively;
5. RegistryDocument is natively consumed as specified;
6. all canonical positives are natively consumable;
7. zero authority-relevant false accepts;
8. zero canonical false rejects;
9. RC3B basis attacks/matrix/type mutations agree;
10. RC3C currentness/wire/delegation/relisted reasons agree where normative;
11. RC3D native interface outcomes/reasons agree where normative;
12. frozen evaluator materialization requires no semantic repair;
13. semantic payload metamorphism causes zero authority-signature changes;
14. no post-reveal repair or bespoke adapter is required.

Use one terminal disposition:

- `SUPPORTED FOR PROMOTION`
- `FALSIFIED`
- `INCONCLUSIVE`
- `SUPERSEDED`

---

## 13. Bounded claim

A complete pass would establish only:

> A fresh Gemini-family implementation process independently recovered materially agreeing Contract E RC3D authority/warrant and public consumer-interface behavior from the six frozen public specification blobs, without pre-freeze access to the hidden evaluator or prior reproductions.

It would not establish:

- Contract E 1.0.0;
- a universal authority theory/evaluator;
- production trust roots;
- production delegation policy;
- correctness of CAL, Evidence Bundler, or Decision Engine semantics;
- safe autonomous production execution.
